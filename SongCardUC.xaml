<UserControl x:Class="MusicSchoolWpf.SongCardUC"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MusicSchoolWpf"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="Wheat">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Image x:Name="pic"/>
        <TextBlock Text="name" Grid.Row="1" HorizontalAlignment="Left" VerticalAlignment="Center" x:Name="name"/>
        <TextBlock Text="artist" Grid.Row="2" HorizontalAlignment="Left" VerticalAlignment="Center" x:Name="artist"/>
        <TextBlock Text="language" Grid.Row="2" HorizontalAlignment="Right" VerticalAlignment="Center" x:Name="lan"/>
        <TextBlock Text="genre" Grid.Row="2" HorizontalAlignment="Center" VerticalAlignment="Center" x:Name="genre"/>
        <TextBlock Text="****" Grid.Row="1" HorizontalAlignment="Right" VerticalAlignment="Bottom" x:Name="diff"/>
    </Grid>
</UserControl>
