<!--<Page x:Class="MusicSchoolWpf.metronome"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MusicSchoolWpf"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      Title="metronome">

    <Grid>
        <TextBlock Text="METRONOME" HorizontalAlignment="Center" VerticalAlignment="Top" FontSize="45"></TextBlock>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
        <TextBox x:Name="BpmTextBox" Text="0" HorizontalAlignment="Center" VerticalAlignment="Center" Width="100" Height="40" FontSize="20" TextAlignment="Center" />
            <TextBlock Text="bpm" FontSize="20" HorizontalAlignment="Center"/>
        </StackPanel>

        <Button Content="+"  HorizontalAlignment="Right" VerticalAlignment="Center" Width="40" Height="40" Margin="0,0,150,0" Click="IncreaseBpm"/>

        <Button Content="-" HorizontalAlignment="Left" VerticalAlignment="Center" Width="40" Height="40" Margin="150,0,0,0" Click="DecreaseBpm"/>

        <Button Margin="0,150,0,0"
  IsEnabled="{Binding DataContext.ControlsEnabled, RelativeSource={RelativeSource FindAncestor, AncestorType=Window}}"
  Style="{StaticResource MaterialDesignIconButton}"
  ToolTip="MaterialDesignIconButton">
            <materialDesign:PackIcon
    Kind="Play" />
        </Button>

    </Grid>
</Page>-->




<Page x:Class="MusicSchoolWpf.metronome" Background="White"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MusicSchoolWpf"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      Title="metronome">

    <Grid>
        <!-- כותרת המטרונום -->
        <TextBlock Text="METRONOME" HorizontalAlignment="Center" VerticalAlignment="Top" FontSize="45" Margin="0,20,0,0"/>

        <!-- StackPanel עבור BPM עם טקסט -->
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBox x:Name="BpmTextBox" Text="0" HorizontalAlignment="Center" VerticalAlignment="Center" Width="100" Height="40" FontSize="20" TextAlignment="Center" />
            <TextBlock Text="bpm" FontSize="20" HorizontalAlignment="Center"/>
        </StackPanel>

        <!-- כפתור פלוס -->
        <Button Content="+" HorizontalAlignment="Right" VerticalAlignment="Center" Width="50" Height="50" Margin="0,0,150,0" FontSize="20" Click="IncreaseBpm"/>

        <!-- כפתור מינוס -->
        <Button Content="-" HorizontalAlignment="Left" VerticalAlignment="Center" Width="50" Height="50" Margin="150,0,0,0" FontSize="20" Click="DecreaseBpm"/>

        <!-- כפתור פליי עם אייקון גדול -->
        <Button Margin="0,150,0,0" Click="playbpm"
                IsEnabled="{Binding DataContext.ControlsEnabled, RelativeSource={RelativeSource FindAncestor, AncestorType=Window}}"
                Style="{StaticResource MaterialDesignIconButton}"
                ToolTip="Play">
            <materialDesign:PackIcon Kind="Play" Width="60" Height="60" VerticalAlignment="Center" HorizontalAlignment="Center" />
        </Button>

    </Grid>
</Page>

